<section id="Hero" class="max-w-7xl mx-auto px-4">
  <div class="flex justify-center items-center p-4 text-black my-16">
    <!-- Contenedor con 3 columnas solo en desktop -->
    <div
      class="grid grid-cols-1 lg:grid-cols-3 items-stretch w-full max-w-screen-xl"
    >
      <!-- Texto izquierdo (arriba y alineado a la derecha) -->
      <!-- Texto izquierdo (arriba y alineado a la derecha globalmente) -->
      <div class="flex items-start lg:justify-end pb-4 lg:pr-4">
        <div class="flex flex-col w-32 text-lg">
          <!-- OBJETO alineado a la izquierda -->
          <p class="font-bold text-left">OBJETO</p>

          <!-- ENTORNO alineado a la derecha -->
          <p class="font-bold text-right">ENTORNO</p>
        </div>
      </div>

      <!-- Imagen centrada vertical y horizontalmente -->
      <div class="flex items-center justify-center">
        <img src="/objetos.webp" class="max-h-[80vh] w-auto rounded-lg" />
      </div>

      <!-- Texto derecho (abajo y alineado a la izquierda) -->
      <div class="flex items-end justify-start lg:pl-4">
        <p class="text-right lg:text-left max-w-xs lg:max-w-md">
          UN PROYECTO QUE BUSCA COMPLEMENTAR ESPACIOS CON PROYECTOS
        </p>
      </div>
    </div>
  </div>
</section>
